cupcake:
  handler: ./src/cupcake/handlers/cupcake.handler
  events:
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/test
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/usuario
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/busqueda
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/name-image
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/name-image-categoria
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/name-image-categoria/usuario
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/name-image-estado
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/name-image/usuario
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/name-image-festividad
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/name-image-festividad/usuario
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/name-image-peliculas
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/name-image-peliculas/usuario
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/name-image-info/usuario
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/name-image-info/publico/usuario
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/name-image-info/paquetes/usuario
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/name-image-info/paquetes-faltantes/usuario
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/cupcake
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/busqueda/usuario
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/ramdom/usuario
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/all-image
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/estados
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/logros
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/insertar-cupcake-estados
        method: post
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/actualizar-cupcake-estados
        method: patch
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/ramdom
        method: get
        cors: ${self:custom.corsCommon}
    - http:
        path: ${self:custom.endpointRoot}/${self:custom.cupcakeModule}/name-image-filtros
        method: get
        cors: ${self:custom.corsCommon}