"use strict";

const GetCupcakeRating =
    require(
        "@cupcake/services/cupcakeRating/GetCupcakeRating"
    );

const SaveCupcakeRating =
    require(
        "@cupcake/services/cupcakeRating/SaveCupcakeRating"
    );

module.exports = {
    GetCupcakeRating,
    SaveCupcakeRating
};